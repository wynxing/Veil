# 产品源码

Rust / x64 MSVC。界面是 egui 小面板 + 原生托盘。关屏 APPLY 只发生在 `Veil.Recovery`。首版合同功能已在代码里齐备，预览包可打；不是已发布，机旁仍待装包验收。

```powershell
cargo test --manifest-path src\Cargo.toml
```

REDMI 短时只停内屏已有 Rust 机旁记录；P15 Rust 尚未执行。
