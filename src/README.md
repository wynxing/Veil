# 产品源码

C# / .NET 8 / WPF，仅 x64。这是公开产品实现，不是已发布能力。关屏 APPLY 只发生在 `Veil.Recovery`。

```powershell
dotnet test src\Veil.sln -p:Platform=x64
```

`app/` 调研原型保持冻结，直到本工程在已测配置上完成与探针等价的机旁闭环。P15 短时只停内屏与热键恢复已观察；面板按钮与只停外屏仅系统检查。REDMI 安装器 VDD 与睡醒再关未执行。
