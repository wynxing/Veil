# 产品源码

Rust / x64 MSVC。界面是 egui 小面板 + 原生托盘，不是已发布能力。关屏 APPLY 只发生在 `Veil.Recovery`。

```powershell
cargo test --manifest-path src\Cargo.toml
```

C# 产品源码已移出工作区。C# 机旁报告不得写成 Rust 已过。REDMI 短时只停内屏已有 Rust 机旁记录；P15 Rust 尚未执行。
