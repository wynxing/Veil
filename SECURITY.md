# 安全

Veil 会改变 Windows 显示拓扑。关光全部物理屏时，它会安装或启用已签名的 MTT 显示驱动。

## 安装包

公开发布的安装包目前没有 Authenticode。只从本仓库的 GitHub Releases 下载 `VeilSetup-*-x64.exe`，并核对同一页的 `SHA256SUMS.txt`。SmartScreen 或「未知发布者」警告是无签名预览的预期。

应用内更新只打开本仓库的 Release 页面，不下载、不启动安装包。不要把来源不明的程序当作 Veil 的更新。

## 报告问题

请用 GitHub Issues 说明现象、Windows 版本，以及当时有没有使用辅助输出。不要附上会话目录里的完整设备路径。需要私下报告时，使用本仓库的 Security 公告入口。
