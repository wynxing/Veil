这是 Veil {{INFORMATIONAL}} **无签名预览包**，供私有仓库协作者本机自用。

- 安装包没有 Authenticode。Windows SmartScreen / 未知发布者会拦截，这是预期，不是已通过发布门禁。
- **不得标为可公开安装**，也不是公开产品已发布。
- 仅面向 **Windows 11 x64**。Windows 10、ARM 与其它机器未测，不得写入支持列表。
- 本版本包含可靠性修复；代码与离线验证不代表机旁验收通过。
- 已记录过机旁/系统检查的机器：XIAOMI REDMI Book 14 2025（Rust 短时只停内屏，沿用已装 VDD）、COLORFUL P15 24（Python 探针，实体外接）。不是全平台兼容。Rust MSI 尚未机旁重装。
- 自带 MTT 是显示驱动，用于没有外接屏时关掉笔记本屏幕。安装时不同意则不要装 VDD。
- 产物：`{{SETUP_FILE}}`（Rust x64 MSVC + WiX，无 .NET 运行时）。请核对本 Release 的 `SHA256SUMS.txt`。

流程与延后项见仓库 `doc/RELEASE.md`。

## 本版本修复

- 关屏、追加关闭、单屏恢复或醒后再关失败后结束关闭要求，执行恢复；失败时保留显式重试入口。
- 协议 v2 使用请求编号和结构化恢复结果，修复重复执行、连续退出绕过和控制文件写入错误。
- VDD 按本次启用责任清理，缺少活动物理输出时保留；卸载检查恢复退出码，并通过维护锁阻止并发关屏。
- 旧协议或损坏的历史会话保持未知；已有 VDD 不自动认领。
- 96 项离线测试及全目标编译已通过；REDMI/P15、睡醒、崩溃和安装卸载仍需机旁验收，详见 doc/validation/reliability-v2.md。
